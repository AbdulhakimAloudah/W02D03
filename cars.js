class car {
  constructor(model, colour, image, registration, number, price) {
    this.model = model;
    this.colour = colour;
    this.image = image;
    this.registration = registration;
    this.number = number;
    this.price = price;
  }
  editPrice(newPrice) {
  this.price = newPrice}
showAll(){
    return 'The ${thes.name} is ${this.editPrice}rial'
}
 
};
  
//-------

